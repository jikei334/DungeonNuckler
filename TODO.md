# ダンジョンナックラー 実装指示書

Claude Codeでの実装を想定した仕様書。技術的な裁量は実装側に委ねるが、ゲームルールの根幹（ターン制・タイマー・敵の攻撃予告）は変更しないこと。

---

## 0. コンセプト概要

- **見た目**: Wizardryのような俯瞰・グリッドベースのダンジョンマップ（3D一人称視点ではなく、2Dトップダウンで常にマップ全体〜プレイヤー周辺が見えている「静的」表示）。ただし**一度も視界に入っていないマスは非表示**（フォグオブウォー、詳細は2.4）
- **生成**: フロアごとにランダム生成（部屋＋通路方式）
- **進行**: ターン制、1人プレイ
- **戦闘**: ポケモン不思議のダンジョン方式。専用戦闘画面には遷移せず、マップ上で攻撃コマンドを出してそのまま解決する
- **緊張要素**: 階層が深くなるほど、1ターンに行動を選べる制限時間が短くなる。時間切れは「棒立ち（行動放棄）」
- **回避要素**: 敵の攻撃対象マスは数ターン前から予告表示され、発動直前のターンで特に強調される（範囲攻撃を避ける猶予を作る）
- **強化**: レベルアップのみ（装備・スキル選択などは無し）
- **攻撃**: プレイヤーは目の前（隣接マス）の敵に殴りかかるのみ。遠距離・魔法などは無し

未確定事項は「assumption」として明記し、実装側の判断に委ねる。

---

## 1. 技術スタック

- **フレームワーク**: Phaser 3 (最新安定版)
- **言語**: TypeScript
- **ビルド**: Vite
- **デプロイ**: GitHub Pages（`vite build` の成果物を `gh-pages` ブランチ or GitHub Actions で公開）
  - `vite.config.ts` の `base` をリポジトリ名に合わせて設定すること（例: `/dungeon-game/`）
  - GitHub Actions での自動デプロイ（`peaceiris/actions-gh-pages` 等）を推奨。手動 `gh-pages` パッケージでも可
- **アセット**: 初期は自作の簡易矩形/シンプル図形スプライトで可（フリー素材の当てはめは後回しでよい）。Kenney.nl等のCC0タイルセットを使う場合はライセンス表記を README に記載

---

## 2. マップ生成（フロア生成）

### 2.1 アルゴリズム
- グリッドサイズ: 1フロア 40×30 マス程度（要調整可能なパラメータとして実装）
- 生成方式: **部屋をランダム配置 → 部屋同士を通路（L字またはランダムウォーク）で接続**するシンプルなアルゴリズムでよい（BSP分割でも可、実装コスト優先で判断してよい）
- 各フロアに以下を配置:
  - プレイヤー開始地点（入口）
  - 階段（次フロアへの出口）1箇所
  - 敵の初期配置（部屋ごとに0〜N体、フロアが深いほど数・強さが増加）
  - 一定間隔（例: 5階層ごと）は**ボスフロア**とし、通常の雑魚とは別枠で強力な固定敵を1体配置する（詳細は6.3）
  - （stretchゴール）アイテムやトラップは今回のMVPスコープ外。将来拡張として構造だけ空けておく

### 2.2 データ構造（例）
```typescript
type TileType = 'wall' | 'floor' | 'stairs';

interface DungeonFloor {
  floorNumber: number;
  width: number;
  height: number;
  tiles: TileType[][];
  playerStart: { x: number; y: number };
  stairsPos: { x: number; y: number };
  enemies: EnemySpawn[];
}
```

### 2.3 再現性
- floorNumberとseedからフロアを生成できるようにする（デバッグ・再現性のため乱数はseed付きPRNGを使用。`Math.random()`直書きは避ける）

### 2.4 視界システム（フォグオブウォー）

マップ自体はフロア生成時に全体を確定させるが、**プレイヤーが一度も視界に入れたことのないマスは描画しない**（黒塗り/非表示）。

#### 視界の形状
- プレイヤーの向いている方向（＝直近の移動方向。初期値は下向きなど任意）を基準に、**前方に開いた三角形（扇形/コーン状）の視界**を持つ
- パラメータ例（要調整）:
  - 視界距離: 5〜6マス
  - 開き角: 90°前後（前方1マスは足元付近まで含む、遠くに行くほど横幅が広がるイメージ）
- 真後ろ・真横は基本的に見えない想定（プレイヤーは「正面しか見ていない」）。この点は「壁の中を覗けてしまう」不自然さを避けるため、後述の壁による遮蔽と組み合わせて調整すること

#### 壁による遮蔽
- コーン範囲内であっても、壁で遮られたマスの先は見えない（プレイヤー座標から対象マスへの直線上に壁があれば非表示のままにする、簡易なレイ判定でよい。本格的なshadowcastingアルゴリズムまでは不要）

#### 表示の3状態（推奨）
1. **未探索（unseen）**: 一度も視界に入っていない → 完全非表示（黒塗り）
2. **探索済み・現在見えていない（explored）**: 過去に見たことはあるが今は視界コーン外 → マップ形状（床/壁）はグレーアウト等で薄く表示し続けてよい（敵・アイテムなどの動的な要素は表示しない）
3. **視界内（visible）**: 現在コーン内かつ遮蔽されていない → 通常表示

　※ 3状態にせず「見た経験があれば常時フル表示」というシンプルな2状態でも、ユーザーの要望上は成立する。実装コストと相談してどちらでもよいが、緊張感を出すなら3状態（今見えているかどうかの区別）を推奨する。

#### 動的要素との連動
- **敵**: 視界コーン内かつ遮蔽されていない場合のみ描画する（探索済みだが視界外のマスにいる敵は表示しない）
- **敵の攻撃予告（5章のテレグラフ）**: 現在視界内のマスに出ている予告のみプレイヤーに見える。視界外で進行している予告があっても、プレイヤーからは見えないまま攻撃が発動しうる（＝視界に入っていない場所は油断できない、という緊張感を意図的に残す）
- 敵側のAI判定（索敵・追跡）自体はプレイヤーの視界とは独立して内部的に計算してよい（＝敵はプレイヤーが見ていなくても普通に行動する）

#### 更新タイミング
- プレイヤーが移動する/向きが変わるたびに視界コーンを再計算し、新たに視界に入ったマスを「探索済み」としてマークする

---

## 3. ターン制システム

### 3.1 基本フロー
1. プレイヤーの入力待ち（移動・攻撃・待機のいずれかを選択）
2. プレイヤーの行動を解決
3. 敵側の行動フェーズ:
   - 各敵のAI状態を更新（索敵→追跡→予告→攻撃実行、詳細は5章）
   - 敵の移動・攻撃を解決
4. HP・状態異常などのチェック（死亡判定、ゲームオーバー判定）
5. 1へ戻る

### 3.2 行動の種類（プレイヤー）
- **移動**: 4方向（上下左右）。8方向にするかは実装側判断でよいが、隣接判定（攻撃対象になるマス）と整合を取ること
- **攻撃**: 目の前（現在向いている方向、または移動先に敵がいる場合のバンプアタック）の隣接マスに敵がいれば攻撃。**遠隔攻撃・範囲攻撃・魔法は実装しない**
  - 実装方式は「移動先に敵がいたら自動的に攻撃扱いにする（バンプアタック）」を推奨（PMD/多くのローグライクの標準UX）。別途「攻撃」コマンドボタンを設けて方向指定させる方式でも可
- **待機**: その場に留まり1ターン消費（HP微回復など無し、シンプルなターン消費でよい）

---

## 4. 行動制限タイマー

### 4.1 仕様
- プレイヤーの行動選択（3.2の入力）に対して、フロアごとに制限時間を設ける
- フロアが深くなるほど制限時間は短縮される
- 制限時間内に行動を選ばなかった場合 → **「棒立ち」= 待機と同じ扱いで1ターン消費**（敵はその間も行動する）

### 4.2 制限時間の計算式（例、要調整）
```
timeLimitMs(floor) = max(
  MIN_TIME_MS,           // 下限（例: 1500ms）
  BASE_TIME_MS - (floor - 1) * DECAY_MS   // 例: 6000 - (floor-1)*300
)
```
- `BASE_TIME_MS`, `DECAY_MS`, `MIN_TIME_MS` は定数として一箇所にまとめ、後で調整しやすくすること
- タイマーはUI上にバー or 数字カウントダウンで常時表示

### 4.3 注意点
- タイマーは「入力を受け付けている間」のみカウントし、演出（攻撃モーション、敵移動アニメーション）中は止める
- ポーズ機能は無くてよいが、タブ非アクティブ時にタイマーが暴走しないよう `document.visibilitychange` などで一時停止する配慮はあると良い（assumption: MVPでは省略可）

---

## 5. 敵AIと攻撃予告システム

これが本ゲームの核となる仕組み。丁寧に実装すること。

### 5.1 敵の状態遷移
```
IDLE（索敵）
  → プレイヤーを検知（視界内 or 一定距離内）
CHASE（追跡）
  → プレイヤーが攻撃範囲に入る/入る見込みになったら
TELEGRAPH（攻撃予告）
  → 指定ターン数経過後
EXECUTE（攻撃実行）
  → クールダウン
COOLDOWN
  → IDLE or CHASEに戻る
```

### 5.2 予告（テレグラフ）の仕様
- 敵は攻撃を出す**Nターン前に対象マス（1マスでも範囲でも可）を決定し、マップ上にマーキング**する
- 予告の見た目は経過ターンに応じて変化させる:
  - 予告開始〜1ターン前: 薄い警告表示（半透明の色付きタイル等）
  - **発動直前のターン（最終予告ターン）: 明確に強調表示**（点滅、濃い色、アイコン追加など）
- 発動ターンでプレイヤーが対象マスにいればダメージを受ける。マス外に逃げていれば回避成立
- 敵タイプによって`telegraphTurns`（予告ターン数）と`attackPattern`（対象マスの形状: 単体・直線・十字範囲など）を変える設計にする
  - 雑魚の通常近接攻撃: `telegraphTurns = 1`（次ターンに攻撃、それでも予告は出す＝「直前でわかる」を満たす最低ライン）
  - 強めの敵・範囲攻撃持ち: `telegraphTurns = 2〜3`

### 5.3 データ構造（例）
```typescript
interface EnemyAttackTelegraph {
  targetTiles: { x: number; y: number }[];
  turnsUntilExecute: number; // カウントダウン。0になったら実行
  pattern: 'single' | 'line' | 'cross' | 'area';
}

interface Enemy {
  id: string;
  pos: { x: number; y: number };
  hp: number;
  atk: number;
  def: number;
  state: 'idle' | 'chase' | 'telegraph' | 'execute' | 'cooldown';
  telegraph?: EnemyAttackTelegraph;
}
```

### 5.4 assumption
- 「視界」判定は単純な距離＋直線遮蔽チェック程度でよい（厳密なFOVアルゴリズムまでは不要）
- 敵が複数同時に予告を出している場合、マス同士が重なっても構わない（重なった場合の特別演出は不要）

---

## 6. 戦闘・ダメージ・HP

### 6.1 プレイヤーHP
- **プレイヤーの最大HPは3**（定数として実装し、後で調整しやすくする。要調整前提）
- 敵の攻撃が命中した場合、攻撃の種類（雑魚の通常攻撃/ボスの範囲攻撃など）を問わず**固定1ダメージ**とする（シンプルさ優先。命中判定さえ正しければダメージ計算に幅を持たせる必要は今のところ無い）
- HPが0になったらゲームオーバー
- プレイヤーに「防御力（DEF）」の概念は無し（固定1ダメージ制のため不要）
- **回復手段は現時点で未確定**。MVPでは実装しない（回復アイテムやフロア踏破時全回復などは将来検討候補として名前だけ残すが、今は着手しない）
- 2.4の視界システムと5章のテレグラフ強調表示は引き続き最重要。HPが3あっても「見えていれば避けられたはずのミス」を減らす方向で作り込むこと

### 6.2 敵へのダメージ計算（プレイヤー→敵）
```
damage = max(1, playerAtk - enemyDef + randomVariance(-1, +1))
```
- プレイヤーのATKはレベルアップで上昇する（7章）
- 敵側は通常通りHPを持ち、0になったら撃破・EXP獲得（6.3でHP設計を詳述）

### 6.3 モンスターHP（設計方針）

ボスとそれ以外で、階層が進むにつれての「体感の変化」を意図的に逆方向にする。

#### 雑魚敵（ボス以外）
- 設計意図: フロアが進むほど、レベルアップで伸びたプレイヤーのATKに対して**相対的に脆くなる**＝「自分が強くなった実感」を出す
- 実装方針: 雑魚のHPはごく緩やかにしか増やさない一方、プレイヤーのATK成長カーブはそれより急にする。結果として「撃破に必要な攻撃回数」が階層が進むごとに自然と減っていく
- 例（あくまで初期値、実プレイして調整する前提）:
  ```
  regularEnemyHP(floor) = round(baseEnemyHP * (1 + floor * 0.05))   // 緩やかな増加
  playerAtk(level)      = baseAtk + level * atkGrowthPerLevel        // レベルは敵撃破で上昇
  ```
  - この2つの伸び方の差によって「浅い階では3発必要だった雑魚が、深い階では1発で倒せる」という体感を作る
  - `baseEnemyHP` / `atkGrowthPerLevel` は実プレイしながら調整するパラメータとして一箇所にまとめること

#### ボス
- 一定間隔（例: 5階層ごと）に出現する固定の強敵
- **重要**: ボスは「最終目標」ではなく、周期的に登場する強めのイベント敵という位置づけ。倒しても即ゲームクリアにはならず、倒した後も通常通り階段から次のフロアへ進み、また5階層後に次のボスと遭遇する、というループになる（8章のエンドレス進行と整合させること）
- 意図的にHPの伸びをプレイヤーのATK成長より急にし、「倒すのにかかる手数が階層を追うごとに徐々に増えていく」ことで歯ごたえを維持する
- 例:
  ```
  bossHP(bossIndex) = baseBossHP * (bossGrowthRate ^ (bossIndex - 1))
  // bossIndex は「何体目のボスか」（1体目、2体目…）
  // bossGrowthRate は雑魚側の増加率よりも明確に大きい値にする
  ```
- ボスのテレグラフは通常敵より複雑にしてよい（範囲を広げる、複数パターンを組み合わせるなど）が、MVPでは5章の通常のテレグラフ機構をそのまま流用するだけでも成立する
- ボスの出現に伴い、2.1のフロア生成にボスフロアの概念を追加する必要がある（下記参照）

### 6.4 表示
- 敵HPバーをスプライト上部に表示
- プレイヤーHPはハート/ダメージアイコン等で3段階表示する（1回被弾ごとに1つ欠ける、といった視覚的にわかりやすい形を推奨）
- 戦闘ログ（簡易テキスト）を画面下部などに数行表示すると分かりやすい（例:「スライムに3ダメージ！」「食らった…残りHP1」）

---

## 7. レベルアップ

- 敵を倒すとEXPを獲得 → 一定値でレベルアップ
- レベルアップ時に上昇するのは**ATK（攻撃力）のみ**。プレイヤーHPは3固定（6.1）で、レベルアップによる最大HP上昇は行わない（assumption。回復手段同様、将来検討候補として名前だけ残す）
- ATKの成長カーブは6.3の雑魚HP・ボスHPとのバランスが要。雑魚に対しては相対的に急な成長、ボスに対しては追いつかれない程度の成長、という2つの制約を同時に満たす必要がある点に注意して数値設計すること
- スキル選択・装備選択などのプレイヤーによる強化選択肢は**実装しない**

---

## 8. フロア送り・ゲームオーバー

- 階段に乗る（または決定キー）で次フロアへ。新フロアを生成し、タイマー制限時間を短縮した状態で再開
- プレイヤーHPが0になったらゲームオーバー画面へ
  - **assumption**: ローグライクとして周回開始（1階から再スタート、パーマデス）をデフォルトとする。セーブ/コンティニューは無し。ここは異論があれば変更可能
- ゴール条件: assumption として「エンドレス（何階まで潜れるかを競う）」とし、リザルト画面で到達階層とレベルを表示する。**ボス（6.3）は5階層ごとに周期的に登場する強敵イベントであり、特定のボスを倒すことがゲームクリア条件になるわけではない**点に注意。ボスを倒した後も通常の雑魚フロアへ進行が続き、次のボスまでまた潜っていく

---

## 9. UI要件まとめ

- ダンジョンマップ（メイン画面）
- プレイヤー/敵のHPバー
- 行動制限タイマー（バー or カウントダウン数字）
- レベル・EXP表示
- 簡易戦闘ログ
- 視界外・未探索エリアの非表示（フォグオブウォー、2.4参照）
- タイトル画面（スタートのみで可）
- ゲームオーバー/リザルト画面（到達階層・レベル表示、リトライボタン）

---

## 10. 実装フェーズ（Claude Codeへの発注単位）

Claude Codeにはこの順で段階実装させることを推奨。各フェーズごとに動作確認してからコミットする。

1. **Phase 0**: Vite + Phaser3 + TypeScript雛形作成、GitHub Pages用ビルド・デプロイ設定
2. **Phase 1**: フロア生成アルゴリズム実装＋タイルマップ描画（歩行のみ、敵無し）
3. **Phase 2**: プレイヤー移動＋ターン制の基盤（1行動=1ターン消費の仕組み）
4. **Phase 3**: 行動制限タイマー実装（棒立ち処理含む）
5. **Phase 4**: 敵配置＋基本AI（索敵・追跡のみ、攻撃はまだ無し）
6. **Phase 5**: 攻撃予告システム（テレグラフ表示・強調演出）
7. **Phase 6**: 戦闘解決（ダメージ計算・HP・撃破・EXP・レベルアップ）
8. **Phase 7**: 階段・フロア送り・タイマー短縮の連動、ゲームオーバー/リザルト
9. **Phase 8**: UI仕上げ（ログ、タイトル画面、演出調整）
10. **Phase 9**: GitHub Pagesへの実デプロイ確認

---

## 11. 明示しておくべき非対応事項（スコープ外）

- 遠距離攻撃・魔法・アイテム使用
- 装備・スキルツリーによる強化
- マルチプレイ
- セーブ/ロード（パーマデス前提）
- 本格的なshadowcastingによる視界計算・光源表現（2.4の簡易コーン判定で十分。厳密な光の反射・減衰表現などは不要）

これらは将来拡張として構造上の余地は残してよいが、**MVPでは実装しない**こと。
